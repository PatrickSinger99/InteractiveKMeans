from k_means import KMeans
from display import Display
import random


if __name__ == '__main__':
    """
    obs = [[random.randint(0, 500) for _ in range(2)] for _ in range(50)]
    kmeans = KMeans(obs, k=4)
    kmeans.set_classes()
    """

    display = Display()
    display.mainloop()
